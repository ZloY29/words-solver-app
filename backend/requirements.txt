Flask==3.1.0
gunicorn==23.0.0

numpy>=1.26,<2
opencv-python-headless>=4.8,<5
Pillow>=10,<12

python-dotenv>=1.0,<2

# TensorFlow (base package; recommended by Apple for TF 2.13+ on macOS)
tensorflow==2.18.0

# For loading .h5 models (usually pulled in by TF, but explicit is safer)
h5py>=3.10,<4

# Optional: GPU acceleration on Apple Silicon (training-heavy; ok to skip for inference)
tensorflow-metal; platform_system == "Darwin" and platform_machine == "arm64"
